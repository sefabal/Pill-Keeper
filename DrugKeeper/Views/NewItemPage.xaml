<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="DrugKeeper.Views.NewItemPage"
             Title="New Item">
    
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Cancel" Clicked="Cancel_Clicked" />
        <ToolbarItem Text="Save" Clicked="Save_Clicked" />
    </ContentPage.ToolbarItems>
    
    <ContentPage.Content>
        <StackLayout Spacing="20" Padding="15" BackgroundColor="AntiqueWhite">
            <Label FontAttributes="Bold" Text="Pill Name" FontSize="Medium" />
            <Entry Text="{Binding Item.Name}" d:Text="Pill Name" FontSize="Small" />
            <Label FontAttributes="Bold" Text="Details" FontSize="Medium" />
            <Editor Text="{Binding Item.Details}" d:Text="Pill details" FontSize="Small" Margin="0" />
            <Label FontAttributes="Bold" Text="Side Effects" FontSize="Medium" />
            <Editor Text="{Binding Item.SideEffects}" d:Text="Item description" FontSize="Small" Margin="0" />
        </StackLayout>
    </ContentPage.Content>
    
</ContentPage>
